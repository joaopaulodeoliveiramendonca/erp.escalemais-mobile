<?xml version="1.0" encoding="utf-8"?>
<!--
  Somente HTTPS, validado pelas autoridades certificadoras do sistema.
  Certificados instalados pelo usuário só valem em build de debug
  (permite inspecionar tráfego com proxy durante o desenvolvimento).
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
